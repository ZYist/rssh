---
phase: 01-broadcast-ui-state
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/ai/ChatPanel.svelte
  - src/lib/i18n/locales/en.ts
  - src/lib/i18n/locales/zh.ts
autonomous: true
requirements:
  - BCAST-01
  - BCAST-02
  - BCAST-03
  - BCAST-04
user_setup: []
must_haves:
  truths:
    - "用户可在 AI 面板工具栏点击广播开关按钮切换广播模式 on/off（BCAST-01, D-07）"
    - "广播 ON 时工具栏下方出现可折叠目标条，列出除主标签外的所有已连接终端（BCAST-02, D-02, D-05）"
    - "用户可勾选/取消勾选任意终端标签作为广播目标，选中态 accent 光晕（BCAST-03, D-01 halo 复用）"
    - "广播开关状态与目标选择按 tab 持久化于 ai store，切 tab / 关重开 AI 面板后保持（BCAST-04, D-10）"
    - "广播 OFF 时 ChatPanel 工具栏与对话区与当前完全一致（广播兼容性全局约束）"
    - "广播开关激活态用 --accent（非 --error 红色，D-08），徽标显示已选目标数（D-09）"
    - "已选目标 tab 被关闭时自动从选择剔除（D-11 prune $effect）；主标签关闭时整条广播状态清理（Plan 01-01 stopSession）"
    - "Serial/Telnet 与 SSH/local 一视同仁列出可勾选（D-06）；新开终端 tab 不自动勾选（D-12）"
  artifacts:
    - "src/lib/ai/ChatPanel.svelte 新增 .broadcast-toggle 工具栏按钮（clear 与 DangerModeToggle 之间）+ .broadcast-bar 可折叠目标条（.banner 之后 .chat 之前）+ prune $effect + 本地 $derived"
    - "src/lib/i18n/locales/en.ts 新增 10 个 ai.toolbar.broadcast_* / ai.broadcast.* key"
    - "src/lib/i18n/locales/zh.ts 同步同批 10 个 key（双语 lockstep）"
  key_links:
    - "ChatPanel `tabId` prop（= aiActiveTab.id）即主标签 id，D-05 过滤键：app.connectedSessions().filter(s => s.tabId !== tabId)"
    - "ChatPanel prune $effect 调 ai.pruneBroadcastTargets(tabId, activeTabIds)（store 不持 $effect，Pitfall 2）"
    - "目标条内 <BroadcastTargetSelector> 的 onToggle/onSelectAll/onSelectNone 回调绑定到 ai.toggleBroadcastTarget / ai.setBroadcastTargets（Plan 01-01 导出）"
    - "i18n key 命名空间 ai.toolbar.broadcast_* 与 ai.broadcast.*，与既有 ai.toolbar.danger_* / ai.session.* 同构"
---

<objective>
在 ChatPanel 工具栏插入广播开关按钮（clear 与 DangerModeToggle 之间，D-07），工具栏下方插入可折叠目标条（D-02 内联条 / D-03 可折叠），接线 ai store 的广播状态（Plan 01-01 已就位），并同步 en.ts + zh.ts 双语 i18n key。完成后用户可在 AI 面板开启广播、勾选目标终端、切 tab/关重开面板后状态保持 —— Phase 1 全部 4 条 Success Criteria 达成。

Purpose: 这是 Phase 1 垂直切片的用户可见半段。Plan 01-01 铺好了 state 层与共享组件；本计划把它们接到 ChatPanel，交付"用户能开关广播 + 选目标"的完整能力。完成后广播模式 UI 全功能可用（命令分发本身是 Phase 2）。
Output: ChatPanel 新增广播开关 + 可折叠目标条 + prune 反应；en.ts/zh.ts 新增 10 个 i18n key；端到端验证广播 OFF 一致性与 per-tab 持久化。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-broadcast-ui-state/01-CONTEXT.md
@.planning/phases/01-broadcast-ui-state/01-RESEARCH.md
@.planning/phases/01-broadcast-ui-state/01-PATTERNS.md
@.planning/phases/01-broadcast-ui-state/01-UI-SPEC.md
@.planning/phases/01-broadcast-ui-state/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPanel.svelte 广播开关按钮 + 可折叠目标条 + prune $effect + CSS（D-02, D-03, D-05, D-06, D-07, D-08, D-09, D-11, D-12）</name>
  <files>src/lib/ai/ChatPanel.svelte</files>
  <read_first>
    - src/lib/ai/ChatPanel.svelte:1-20（imports + props：tabId 即主标签 id，D-05 过滤键）
    - src/lib/ai/ChatPanel.svelte:245-302（工具栏 DOM：model(250) → tokens(251) → audit-btn(257) → clear-btn(273) → [广播开关插入点: 282 与 287 之间] → DangerModeToggle(287-300) → close-btn(301)）
    - src/lib/ai/ChatPanel.svelte:304-314（.banner 条件渲染 304-309 + {#if auditOpen && session}/.chat 311-314 —— 目标条插入点：.banner 之后、audit/chat 之前）
    - src/lib/ai/ChatPanel.svelte:460-497（.btn-icon 基础样式 460-476 + .danger-toggle.on 480-487 tint 节奏 + .banner 488-497 flex-shrink:0 先例）
    - src/lib/ai/DangerModeToggle.svelte:25-48（toggle + snippet trigger + aria-pressed 先例 —— 广播开关结构仿此但无 confirm modal、无 disabled、激活用 accent）
    - src/lib/components/EditPane.svelte:23-27（prune $effect 先例：跟踪 connectedSessions() 变化剔除失效 tabId）
    - src/lib/stores/app.svelte.ts:406-413（SessionEntry/SessionInfo 类型：type 字段为 ssh|local|serial|telnet）
    - src/lib/stores/app.svelte.ts:456（connectedSessions() 返回 SessionInfo[]，D-05 过滤数据源）
    - .planning/phases/01-broadcast-ui-state/01-RESEARCH.md §Architecture Patterns §Pattern 3（ChatPanel 工具栏 + 目标条插入点）+ §Common Pitfalls §Pitfall 2/3/6
    - .planning/phases/01-broadcast-ui-state/01-PATTERNS.md §Pattern Assignments §MODIFY ChatPanel.svelte（toggle button skeleton + collapsible bar skeleton + CSS）
    - .planning/phases/01-broadcast-ui-state/01-UI-SPEC.md §Interaction State Machines（开关/折叠/勾选三状态机）+ §Design Token Grounding（开关激活态 + 徽标 + 目标条容器 token）
  </read_first>
  <action>
    **步骤 A —— imports + 本地 $derived（script 块顶部）：**

    ChatPanel.svelte 既有 imports（:1-11）已有 `ai`、`t`、`errMsg`。新增：
    - `import * as app from "../stores/app.svelte.ts";`（为 connectedSessions()，D-05 数据源）
    - `import BroadcastTargetSelector from "../components/BroadcastTargetSelector.svelte";`（Plan 01-01 产物）

    新增本地 `$derived`（沿既有 ChatPanel $derived 风格）：
    - `let bState = $derived(ai.broadcastState(tabId));`（per-tab 广播状态快照）
    - `let broadcastOn = $derived(bState.enabled);`
    - `let selectedCount = $derived(bState.targets.size);`
    - `let sessions = $derived(app.connectedSessions().filter(s => s.tabId !== tabId));`（per D-05 过滤主标签；tabId 是 ChatPanel prop 即主标签 id）
    - `let totalCount = $derived(sessions.length);`

    **步骤 B —— prune $effect（script 块，镜像 EditPane.svelte:23-27，per D-11）：**

    在 script 块内（组件 init 上下文，Pitfall 2 合规位置）加：
    `$effect(() => { const activeIds = new Set(sessions.map(s => s.tabId)); ai.pruneBroadcastTargets(tabId, activeIds); });`
    该 $effect 跟踪 sessions 变化（目标 tab 打开/关闭），调 Plan 01-01 的 pruneBroadcastTargets mutator 剔除已关闭的目标 tabId。ChatPanel unmount（AI 面板关闭）时 $effect 销毁，但状态在模块级 store 存活，面板重开 $effect 重生（Pitfall 3 合规）。

    **步骤 C —— 工具栏广播开关按钮（per D-07 位置 / D-08 accent / D-09 徽标）：**

    在 ChatPanel.svelte:282（clear-btn 结束 `</button>`）与 :287（`<DangerModeToggle>`）之间插入广播开关。**不**包 DangerModeToggle wrapper（广播无 confirm modal，per D-04 + UI-SPEC §State Machine §1"无 confirm modal"）；**不**设 `disabled`（广播是 per-tab UI 状态，session 启动前后均可设，UI-SPEC §State Machine §1"永不 disabled"）。

    按钮 markup（镜像 :289-298 DangerModeToggle trigger 结构，换图标 + 换 accent + 加徽标）：
    - `class="btn-icon broadcast-toggle"` + `class:on={broadcastOn}`
    - `onclick={() => ai.toggleBroadcast(tabId)}`
    - `title={broadcastOn ? t("ai.toolbar.broadcast_on_tip") : t("ai.toolbar.broadcast_enable")}`
    - `aria-label={broadcastOn ? t("ai.toolbar.broadcast_aria") + ", " + selectedCount + "/" + totalCount : t("ai.toolbar.broadcast_aria")}`（ON 态含计数，UI-SPEC §A11y）
    - `aria-pressed={broadcastOn}`
    - 内含 `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">` —— 图标 path 从 lucide.dev 复制 `radio` 图标的 canonical path d 值（A2 [ASSUMED]，实现时打开 https://lucide.dev/icons/radio 复制其 SVG path；不装 lucide-svelte 包）。
    - 徽标：`{#if broadcastOn && selectedCount > 0}<span class="badge" aria-hidden="true">{selectedCount}</span>{/if}`（0 目标时隐藏，UI-SPEC §Token Grounding 锁定）。

    **步骤 D —— 可折叠目标条（per D-02 内联条 / D-03 可折叠）：**

    在 ChatPanel.svelte:309（`.banner` 的 `{/if}`）之后、:311（`{#if auditOpen && session}`）之前插入：
    `{#if broadcastOn}<div class="broadcast-bar"> ... </div>{/if}`（广播 OFF 整条不渲染，BCAST-02 + 兼容性约束）。

    目标条内部分两层（UI-SPEC §Discretion "折叠机制"锁定）：
    1. **标题条**（始终显示，约 32px 高，保留再展开 affordance）：`.bar-header`，含 `t("ai.broadcast.title")` + `t("ai.broadcast.count", { selected: selectedCount, total: totalCount })`（M=0 时标题条仍显示，但下方列表区显示空状态文案）+ chevron 图标。`onclick={() => ai.setBroadcastBarCollapsed(tabId, !bState.barCollapsed)}`，`role="button" tabindex="0"`，`aria-expanded={!bState.barCollapsed}`，`aria-label={bState.barCollapsed ? t("ai.broadcast.expand") : t("ai.broadcast.collapse")}`。
    2. **列表区**（`{#if !bState.barCollapsed}`）：`<div class="bar-list">` 内放 `<BroadcastTargetSelector sessions={sessions} selectedIds={bState.targets} onToggle={(tid) => ai.toggleBroadcastTarget(tabId, tid)} onSelectAll={() => ai.setBroadcastTargets(tabId, new Set(sessions.map(s => s.tabId)))} onSelectNone={() => ai.setBroadcastTargets(tabId, new Set())} />`。注意 `bState.targets` 作 selectedIds 是只读契约，选择器不得原地改（mutator 经 ai.*）。

    D-05（主标签过滤）已在 sessions $derived 完成；D-06（raw device 照常列出）= 不加额外 type 过滤；D-12（新 tab 不自动勾选）= 不在 $effect 里自动 add，仅 prune 剔除。

    **步骤 E —— `<style>` 新增（per D-08 accent / D-09 徽标 / R4 flex 三件套）：**

    新增 CSS 规则（全部走 design token，per Pr3）：
    - `.broadcast-toggle { position: relative; }`（徽标 absolute 锚点）
    - `.broadcast-toggle.on` —— **逐字镜像 .danger-toggle.on（:480-487），把 danger toggle 用的 error 红 token 全部换成 accent token**（per D-08 严禁红色）：`color: var(--accent); background: color-mix(in srgb, var(--accent) 14%, transparent);`
      <!-- planner-discipline-allow: var(--error) -->
    - `.broadcast-toggle.on:hover` —— 同上换 accent，tint 升到 22%
    - `.broadcast-toggle .badge` —— UI-SPEC §Token Grounding 锁定值：`position: absolute; bottom: -2px; right: -2px; min-width: 16px; height: 16px; padding: 0 4px; border-radius: 50%; background: var(--accent); color: var(--white); font-size: 10px; font-weight: 700; line-height: 1; border: 2px solid var(--bg); display: flex; align-items: center; justify-content: center;`
    - `.broadcast-bar` —— 镜像 .banner（:488-497）族：`flex-shrink: 0; border-bottom: 1px solid var(--divider); background: var(--bg);`
    - `.bar-header` —— 标题行：`display: flex; align-items: center; gap: var(--space-2); padding: var(--space-2) var(--space-3); cursor: pointer;`
    - `.bar-title` —— `font-size: 13px; font-weight: 700; color: var(--text);`（复用 .panel-header 字号字重）
    - `.bar-count` —— `font-size: 12px; color: var(--text-dim);`
    - `.bar-list` —— `max-height: 40vh; overflow-y: auto; padding: 0 var(--space-3);`（UI-SPEC §Responsive 锁定 40vh 上限防吃满对话区，Pitfall 6）
    - chevron：`color: var(--text-sub);`，collapsed 态可旋转（`transform: rotate(-90deg)`，transition 0.15s）
    - **验证**既有 `.chat` 规则保持 R4 三件套（`flex: 1; overflow-y: auto; min-height: 0;`）未被新插入条破坏（Pitfall 6）—— 若 .chat 在 flex 容器内，.broadcast-bar 的 flex-shrink:0 保证不挤压 .chat。

    禁止：用 `--error` 红色（D-08）；用裸 hex（Pr3）；用 `on:click`/`$:`/`export let`（R7）；在组件内建广播状态（R8，状态在 ai store）；引入新 invoke/listen/OSC（R1/R2，本阶段零后端）；为徽标加 `disabled` 或 confirm modal（UI-SPEC §State Machine §1）。
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'broadcast-toggle' src/lib/ai/ChatPanel.svelte` ≥ 3（class 定义 + button class + CSS 规则）
    - `grep -c 'class:on={broadcastOn}' src/lib/ai/ChatPanel.svelte` == 1（开关激活态绑定）
    - `grep -c 'ai.toggleBroadcast(tabId)' src/lib/ai/ChatPanel.svelte` == 1（开关 onclick 接 store）
    - `grep -c 'aria-pressed={broadcastOn}' src/lib/ai/ChatPanel.svelte` == 1（a11y）
    - `grep -c 'BroadcastTargetSelector' src/lib/ai/ChatPanel.svelte` ≥ 2（import + 使用）
    - `grep -c 'app.connectedSessions().filter(s => s.tabId !== tabId)' src/lib/ai/ChatPanel.svelte` == 1（D-05 主标签过滤）
    - `grep -c 'ai.pruneBroadcastTargets(tabId' src/lib/ai/ChatPanel.svelte` == 1（D-11 prune $effect 调 store mutator）
    - `grep -c '\$effect' src/lib/ai/ChatPanel.svelte` ≥ 1（prune $effect 存在于组件 init 上下文）
    - `grep -c 'broadcast-bar' src/lib/ai/ChatPanel.svelte` ≥ 2（条件渲染容器 + CSS）
    - `grep -c 'var(--accent)' src/lib/ai/ChatPanel.svelte` 中 `.broadcast-toggle.on` 规则块内出现（D-08 accent 激活态）
    - `grep -c 'var(--error)' src/lib/ai/ChatPanel.svelte` 不出现在任何 `.broadcast-` 规则内（D-08 严禁红色 —— 用 grep -A 上下文核对，或 `grep -cE 'broadcast-toggle[^}]*--error' src/lib/ai/ChatPanel.svelte` == 0）
    - `grep -c 'class="badge"' src/lib/ai/ChatPanel.svelte` == 1（D-09 徽标）
    - `grep -c 'setBroadcastBarCollapsed' src/lib/ai/ChatPanel.svelte` ≥ 1（D-03 折叠接线）
    - `grep -c 'max-height: 40vh' src/lib/ai/ChatPanel.svelte` ≥ 1（列表区高度上限，Pitfall 6）
    - `npm run build` 退出码 0（Svelte 编译 + ChatPanel 改造无错误）
  </acceptance_criteria>
  <done>ChatPanel 工具栏新增 .broadcast-toggle 开关（clear 与 DangerModeToggle 之间，accent 激活态 + 目标计数徽标）+ .broadcast-bar 可折叠目标条（.banner 之后 .chat 之前）+ prune $effect + D-05 主标签过滤 + D-08 accent（非红）；广播 OFF 时整条不渲染（兼容性约束）；npm run build 通过。</done>
</task>

<task type="auto">
  <name>Task 2: en.ts + zh.ts 同步 10 个广播 i18n key + 端到端验证（broadcast OFF 一致性 + per-tab 持久化）</name>
  <files>src/lib/i18n/locales/en.ts, src/lib/i18n/locales/zh.ts</files>
  <read_first>
    - src/lib/i18n/locales/en.ts:1-20（flat 对象 + dotted-key 结构，无嵌套）
    - src/lib/i18n/locales/en.ts:405-424（`// ── AI Diagnose: ChatPanel ──` 段，ai.toolbar.* / ai.session.* 既有 key 先例，新 key 追加到此段附近）
    - src/lib/i18n/locales/en.ts:415（`ai.toolbar.tokens_tip` 的 `{tin}/{tout}` 插值占位符先例 —— ai.broadcast.count 的 {selected}/{total} 沿用同语法）
    - src/lib/i18n/locales/zh.ts（en.ts 的镜像，同 flat dotted-key 结构，同 key 集）
    - .planning/phases/01-broadcast-ui-state/01-UI-SPEC.md §Copywriting Contract（10 个 key 的 en/zh 锁定文案表）
    - .planning/phases/01-broadcast-ui-state/01-RESEARCH.md §Code Examples §i18n key（flat dotted-key 结构示例）
  </read_first>
  <action>
    在 en.ts 与 zh.ts 的 `ai.toolbar.*` / `ai.session.*` 段附近（en.ts:419 `ai.toolbar.danger_enable` 之后或该段末尾）追加 10 个 key。**两文件必须 lockstep 同 key 集**（CONVENTIONS.md §i18n 双语 key）。文案逐字取自 UI-SPEC §Copywriting Contract 锁定表：

    en.ts 追加：
    - `"ai.toolbar.broadcast_enable": "Enable Broadcast Mode",`
    - `"ai.toolbar.broadcast_on_tip": "Broadcast is ON — approved commands will sync to selected terminals",`
    - `"ai.toolbar.broadcast_aria": "Broadcast mode",`
    - `"ai.broadcast.title": "Broadcast Targets",`
    - `"ai.broadcast.count": "{selected}/{total} targets",`（插值占位符 {selected}/{total} 沿用 tokens_tip 的 {tin}/{tout} 语法）
    - `"ai.broadcast.empty": "No other terminals",`
    - `"ai.broadcast.select_all": "All",`
    - `"ai.broadcast.select_none": "None",`
    - `"ai.broadcast.collapse": "Collapse target bar",`
    - `"ai.broadcast.expand": "Expand target bar",`

    zh.ts 追加同 key 集，中文值（UI-SPEC 锁定）：
    - `"ai.toolbar.broadcast_enable": "开启广播模式",`
    - `"ai.toolbar.broadcast_on_tip": "广播已开启——批准的命令将同步到已选终端",`
    - `"ai.toolbar.broadcast_aria": "广播模式",`
    - `"ai.broadcast.title": "广播目标",`
    - `"ai.broadcast.count": "{selected}/{total} 个目标",`
    - `"ai.broadcast.empty": "没有其它终端",`
    - `"ai.broadcast.select_all": "全选",`
    - `"ai.broadcast.select_none": "全不选",`
    - `"ai.broadcast.collapse": "收起目标栏",`
    - `"ai.broadcast.expand": "展开目标栏",`

    插值 {selected}/{total} 走既有 i18n 参数替换（`t("ai.broadcast.count", { selected, total })`），与 ChatPanel Task 1 的 `t("ai.broadcast.count", { selected: selectedCount, total: totalCount })` 调用对齐。

    完成后运行端到端验证（见 verify）。验证目标：(a) 广播 OFF 时 ChatPanel 工具栏与对话区与改造前像素一致（兼容性全局约束）；(b) 广播 ON 时目标条出现、可勾选、徽标计数正确；(c) 切 tab / 关重开 AI 面板后状态保持（BCAST-04）；(d) 关闭一个已选目标 tab 后该 tabId 自动从选择剔除（D-11）；(e) `npm run build` + `npx tsc --noEmit` + `npm test` 全绿。
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"ai.toolbar.broadcast_enable"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.toolbar.broadcast_on_tip"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.toolbar.broadcast_aria"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.title"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.count"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.empty"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.select_all"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.select_none"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.collapse"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.broadcast.expand"' src/lib/i18n/locales/en.ts` == 1
    - `grep -c '"ai.toolbar.broadcast_enable"' src/lib/i18n/locales/zh.ts` == 1（zh 同步）
    - `grep -c '"ai.broadcast.count"' src/lib/i18n/locales/zh.ts` == 1（zh 同步）
    - en.ts 与 zh.ts 的广播 key 集合相同：`diff <(grep -oE '"ai\.(toolbar\.)?broadcast[^"]*"' src/lib/i18n/locales/en.ts | sort) <(grep -oE '"ai\.(toolbar\.)?broadcast[^"]*"' src/lib/i18n/locales/zh.ts | sort)` 输出为空（双语 lockstep）
    - `npm test` 退出码 0（既有单测无回归）
    - `npx tsc --noEmit` 退出码 0
    - `npm run build` 退出码 0（全量编译含 Svelte + i18n）
  </acceptance_criteria>
  <done>en.ts + zh.ts 同步 10 个 ai.toolbar.broadcast_* / ai.broadcast.* key（双语 lockstep，文案取自 UI-SPEC §Copywriting 锁定表）；npm test + npx tsc --noEmit + npm run build 全绿；Phase 1 端到端验证清单（广播 OFF 一致性、ON 功能、per-tab 持久化、D-11 prune、D-08 视觉）就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无外部信任边界 | 本计划是纯前端 UI 接线 + 静态 i18n 文案，零 invoke/零网络/零 IPC/零用户自由文本输入/零持久化。信任边界不适用。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | 已选目标 tab 被关闭后 tabId 残留于 targets Set（计数错误 + Phase 2 误分发到死 session） | low | mitigate | Task 1: ChatPanel 内 prune `$effect` 跟踪 `app.connectedSessions()` 变化，调 `ai.pruneBroadcastTargets(tabId, activeIds)` 剔除。acceptance criterion grep 验证 pruneBroadcastTargets 调用存在。 |
| T-01-05 | Tampering | 广播激活态误用 --error 红色，与 DangerMode 危险语义混淆，用户误以为广播是破坏性操作 | low | mitigate | Task 1: CSS 强制 `.broadcast-toggle.on` 用 `var(--accent)`，acceptance criterion 负向 grep 确保无 `--error` 出现在 broadcast 规则内（D-08）。 |
| T-01-06 | Elevation of Privilege | 广播开关引入 confirm modal 或 disabled 态，改变既有审批流语义（违反 BCAST-08 前置约束 + D-04） | low | mitigate | Task 1: 开关无 DangerModeToggle wrapper、无 confirm modal、无 disabled（UI-SPEC §State Machine §1 锁定）。broadcast 不触发任何命令执行（分发是 Phase 2），本阶段纯状态。 |
| T-01-07 | Denial of Service | 目标条列表无限增长吃满对话区，用户无法滚动对话 | low | mitigate | Task 1: `.bar-list` 强制 `max-height: 40vh; overflow-y: auto;`（UI-SPEC §Responsive），acceptance criterion grep 验证。 |

本计划无 npm/pip/cargo 安装（零新依赖，RESEARCH §Package Legitimacy Audit N/A），故无 T-XX-SC 供应链威胁条目。
</threat_model>

<verification>
**Phase 1 端到端验证（本计划完成后执行）：**
- `npm test` 通过（既有单测无回归）
- `npx tsc --noEmit` 通过（TS 类型检查）
- `npm run build` 通过（Svelte + i18n 全量编译）
- 广播 OFF 一致性：ChatPanel 工具栏与对话区与改造前一致（`.broadcast-toggle` 不渲染 .on 态、`.broadcast-bar` 整条 `{#if broadcastOn}` 不渲染）
- 广播 ON 功能：开关可点开、目标条出现、可勾选目标、徽标计数 = 已选数、全选/全不选工作
- per-tab 持久化（BCAST-04）：切到另一 tab 再切回，广播开关与目标选择保持；关 AI 面板再开，状态保持（因状态在模块级 `_broadcastByTab`，非组件本地）
- D-11 prune：勾选一个目标 tab → 关闭该 tab → 该 tabId 自动从选择剔除、徽标计数递减
- D-08 视觉：广播激活态是 accent 蓝（--accent #4A6CF7），不是红（--error）
</verification>

<success_criteria>
- 用户可在 AI 面板工具栏点击广播开关切换 on/off（BCAST-01, D-07）
- 广播 ON 时目标条出现，列出除主标签外所有已连接终端，可勾选/取消勾选（BCAST-02, BCAST-03, D-01 halo, D-05 过滤, D-06 raw 照列, D-12 新 tab 不自动勾）
- 广播状态 per-tab 持久化于 ai store，切 tab / 关重开 AI 面板保持（BCAST-04, D-10）
- 广播 OFF 时行为与改造前完全一致（兼容性全局约束）
- 开关激活态 accent 色 + 目标计数徽标（D-08, D-09）
- 已选目标关闭自动 prune；主标签关闭整条状态清理（D-11 双路径，与 Plan 01-01 stopSession 协同）
- en.ts + zh.ts 10 个 i18n key 双语 lockstep（UI-SPEC §Copywriting）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-broadcast-ui-state/01-02-SUMMARY.md`
</output>

<artifacts_produced>
## Artifacts this phase produces（供 plan-review-convergence 漂移验证排除新符号）

**本计划（01-02）新增/修改的符号：**

ChatPanel（src/lib/ai/ChatPanel.svelte）：
- imports: `app` store, `BroadcastTargetSelector`
- 本地 `$derived`: `bState`, `broadcastOn`, `selectedCount`, `sessions`（含 D-05 filter）, `totalCount`
- prune `$effect`（调 ai.pruneBroadcastTargets）
- 工具栏按钮：`.btn-icon.broadcast-toggle`（class:on, aria-pressed, 徽标 .badge）
- 目标条：`.broadcast-bar` > `.bar-header`（标题 + N/M 计数 + chevron）+ `.bar-list`（内含 BroadcastTargetSelector）
- CSS: `.broadcast-toggle` / `.broadcast-toggle.on`（accent tint）/ `.broadcast-toggle .badge` / `.broadcast-bar` / `.bar-header` / `.bar-title` / `.bar-count` / `.bar-list`

i18n（src/lib/i18n/locales/en.ts + zh.ts，双语 lockstep）：
- `ai.toolbar.broadcast_enable`, `ai.toolbar.broadcast_on_tip`, `ai.toolbar.broadcast_aria`
- `ai.broadcast.title`, `ai.broadcast.count`, `ai.broadcast.empty`, `ai.broadcast.select_all`, `ai.broadcast.select_none`, `ai.broadcast.collapse`, `ai.broadcast.expand`

**依赖 Plan 01-01 的符号（已在其 artifacts_produced 登记）：**
- `ai.broadcastState/broadcastEnabled/broadcastTargets/toggleBroadcast/setBroadcastBarCollapsed/toggleBroadcastTarget/setBroadcastTargets/pruneBroadcastTargets/clearBroadcastState`
- `BroadcastTargetSelector` 组件
</artifacts_produced>
