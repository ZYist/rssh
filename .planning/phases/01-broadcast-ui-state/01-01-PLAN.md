---
phase: 01-broadcast-ui-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/store.svelte.ts
  - src/lib/components/BroadcastTargetSelector.svelte
  - src/lib/components/EditPane.svelte
autonomous: true
requirements:
  - BCAST-04
user_setup: []
must_haves:
  truths:
    - "ai/store.svelte.ts 存在按 tabId 索引的 `_broadcastByTab` per-tab 广播状态（D-10）"
    - "广播状态包含 enabled / barCollapsed / targets(Set<string>) 三字段（UI-SPEC 锁定的 BroadcastState 形状）"
    - "所有改 targets 的 mutator 重建 Set + 整体替换 record + 重新赋值 _broadcastByTab（Svelte 5 $state 不拦截 Set 方法，Pitfall 1）"
    - "主标签关闭时 stopSession 清理 _broadcastByTab[tab_id] 整条 record（防泄漏，D-11 主标签路径）"
    - "BroadcastTargetSelector 是受控组件（props in / callbacks out），自身不持状态"
    - "EditPane 改造后其既有 Broadcast(N) 立即发送行为与改造前完全一致（回归安全，广播 OFF 一致性约束）"
  artifacts:
    - "src/lib/components/BroadcastTargetSelector.svelte（新增受控组件）"
    - "src/lib/ai/store.svelte.ts 中新增 _broadcastByTab + BroadcastState + DEFAULT_BROADCAST + 9 个 getter/mutator + stopSession teardown 行"
    - "src/lib/components/EditPane.svelte 改造为消费 BroadcastTargetSelector，保留 .panel-header / .broadcast-btn / hover popover / 本地 selectedTabIds"
  key_links:
    - "BroadcastTargetSelector 接收 selectedIds: Set<string>（只读）+ onToggle/onSelectAll/onSelectNone 回调 —— 宿主负责过滤主标签（D-05 契约）"
    - "store.stopSession delete 簇追加 `delete _broadcastByTab[tab_id];`（与既有 _sessionByTab 等同寿命清理）"
    - "EditPane 的 toggle/selectAll/selectNone 作为 onToggle/onSelectAll/onSelectNone 传入共享组件（本地 selectedTabIds 仍是 EditPane 一次性发送语义，不进 ai store）"
---

<objective>
为 Phase 1 广播 UI 铺设状态地基与可复用的目标选择器组件：在 `ai/store.svelte.ts` 新增 per-tab 广播状态（D-10）及其全部 getter/mutator，从 `EditPane.svelte` 抽取目标列表为受控组件 `BroadcastTargetSelector.svelte`（D-01），并改造 EditPane 消费该组件（保留其 Broadcast(N) 立即发送语义，D-04）。

Purpose: 本计划的产物是 Plan 01-02（ChatPanel 广播开关 + 目标条）的直接依赖。state 层与共享组件就位后，Plan 02 只需在 ChatPanel 接线即可交付用户可见的广播 UI。同时 EditPane 改造为零行为变化的纯重构（回归安全），满足"广播关闭时行为与当前完全一致"的全局约束。
Output: 新增 `BroadcastTargetSelector.svelte`；`store.svelte.ts` 增加 `_broadcastByTab` 状态机；`EditPane.svelte` 消费共享组件且 Broadcast(N) 行为不变。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-broadcast-ui-state/01-CONTEXT.md
@.planning/phases/01-broadcast-ui-state/01-RESEARCH.md
@.planning/phases/01-broadcast-ui-state/01-PATTERNS.md
@.planning/phases/01-broadcast-ui-state/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: store.svelte.ts 新增 per-tab 广播状态 + getter/mutator + stopSession teardown（D-10, D-11, D-12）</name>
  <files>src/lib/ai/store.svelte.ts</files>
  <read_first>
    - src/lib/ai/store.svelte.ts:40-72（_sessionByTab/_pendingByTab/_chatByTab/_tokensByTab 既有 per-tab map 声明先例 + 注释风格）
    - src/lib/ai/store.svelte.ts:103-126（sessionForTab/pendingCommand/tokenUsage getter 命名先例：无 get 前缀、动词短语、不存在返回默认值）
    - src/lib/ai/store.svelte.ts:225-262（stopSession 异步 teardown：255-260 行 delete 簇是追加 `delete _broadcastByTab[tab_id];` 的精确位置）
    - src/lib/ai/store.svelte.ts:285-289（rebindTarget 整体替换 record 条目先例：`_sessionByTab[tab_id] = { ...info, target_id }`）
    - src/lib/components/EditPane.svelte:29-37（toggle/selectAll/selectNone 重建 Set 先例 —— 证明 $state 下 Set 必须整体重赋值）
    - .planning/phases/01-broadcast-ui-state/01-RESEARCH.md §Common Pitfalls §Pitfall 1（$state 不拦截 Set 方法，必须重建）
    - .planning/phases/01-broadcast-ui-state/01-PATTERNS.md §Pattern Assignments §MODIFY store.svelte.ts（mutator 形状模板）
  </read_first>
  <action>
    在 `_pendingByTab` 声明附近（store.svelte.ts:54 之后）新增 per-tab 广播状态，严格沿 `_xByTab` 先例（per D-10）：

    1. 定义 `interface BroadcastState { enabled: boolean; barCollapsed: boolean; targets: Set<string>; }`（三字段形状由 UI-SPEC §Discretion 锁定）。
    2. 声明 `let _broadcastByTab = $state<Record<string, BroadcastState>>({});`。
    3. 声明 `const DEFAULT_BROADCAST: BroadcastState = { enabled: false, barCollapsed: false, targets: new Set() };`（首次开启广播时 barCollapsed=false 即"首次展开"，UI-SPEC 锁定）。

    新增 getter（命名沿既有无 get 前缀规范，per D-10）：
    - `broadcastState(tabId: string): BroadcastState` —— 返回 `_broadcastByTab[tabId] ?? DEFAULT_BROADCAST`（只读快照；调用方不得原地改返回的 Set）。
    - `broadcastEnabled(tabId: string): boolean` —— 返回 `broadcastState(tabId).enabled`。
    - `broadcastTargets(tabId: string): Set<string>` —— 返回 `broadcastState(tabId).targets`（Phase 2 分发时读它传给 broadcastToSessions）。

    新增 mutator —— **每一个都遵循"重建 Set → 整体替换 record 条目 → 重新赋值整个 _broadcastByTab"三步**（per D-10 反应性，Pitfall 1；沿 EditPane.svelte:29-37 + store.svelte.ts:285-289 合并先例）：
    - `toggleBroadcast(tabId: string): void` —— 翻转 enabled，保留 targets 与 barCollapsed。
    - `setBroadcastBarCollapsed(tabId: string, collapsed: boolean): void` —— 设置 barCollapsed。
    - `toggleBroadcastTarget(tabId: string, targetTabId: string): void` —— 重建 targets Set，has 则 delete 否则 add。
    - `setBroadcastTargets(tabId: string, ids: Set<string>): void` —— 全选/全不选用，直接用传入的 ids 作为新 targets（仍需整体替换 record）。
    - `pruneBroadcastTargets(tabId: string, activeTabIds: Set<string>): void` —— per D-11。用 `[...prev.targets].filter(id => activeTabIds.has(id))` 构造新 Set；**若 `next.size === prev.targets.size` 则 early-return 不触发赋值**（防 spurious 反应，Pitfall 1）。record 不存在时 early-return。
    - `clearBroadcastState(tabId: string): void` —— `delete _broadcastByTab[tabId];`（主标签关闭路径，与 D-11 prune 是两条不同路径）。

    stopSession teardown 集成（per D-11 主标签清理路径）：在 store.svelte.ts:255-260 的既有 delete 簇末尾追加 `delete _broadcastByTab[tab_id];`（位于 `delete _tokensByTab[tab_id];` 之后、`if (_activeTabId === tab_id)` 之前）。**禁止**在此文件顶层写 `$effect`（store.svelte.ts 全文零 $effect 调用，所有副作用经 attachListeners；prune $effect 归 ChatPanel，见 Plan 01-02，Pitfall 2）。

    禁止：对 `_broadcastByTab[tabId].targets` 做 in-place `.add()/.delete()`（反应性静默失效）；导出裸 `_broadcastByTab`（R8 状态私有化 + getter 导出）。
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_broadcastByTab" src/lib/ai/store.svelte.ts` ≥ 1（状态声明存在）
    - `grep -c "interface BroadcastState" src/lib/ai/store.svelte.ts` == 1（接口定义存在）
    - `grep -c "DEFAULT_BROADCAST" src/lib/ai/store.svelte.ts` ≥ 2（常量声明 + 至少一处 getter 引用）
    - `grep -cE "export function (toggleBroadcast|broadcastState|broadcastEnabled|broadcastTargets|setBroadcastBarCollapsed|toggleBroadcastTarget|setBroadcastTargets|pruneBroadcastTargets|clearBroadcastState)\b" src/lib/ai/store.svelte.ts` == 9（全部 9 个导出函数存在）
    - `grep -c "delete _broadcastByTab\[tab_id\]" src/lib/ai/store.svelte.ts` == 1（stopSession teardown 追加存在）
    - `grep -c "next.size === prev.targets.size" src/lib/ai/store.svelte.ts` == 1（prune 无变化 early-return 守卫存在）
    - store.svelte.ts 内**无** `$effect` 调用（`grep -c '\$effect' src/lib/ai/store.svelte.ts` == 0，保持既有不变量）
    - `npx tsc --noEmit` 退出码 0（TS 类型检查通过）
    - 所有改 targets 的 mutator（toggleBroadcastTarget / setBroadcastTargets / pruneBroadcastTargets）均包含 `new Set(` 构造与 `_broadcastByTab = { ..._broadcastByTab,` 整体赋值（grep 验证每函数体内同时出现这两串）
  </acceptance_criteria>
  <done>store.svelte.ts 新增 _broadcastByTab per-tab 广播状态 + BroadcastState 接口 + DEFAULT_BROADCAST + 9 个 getter/mutator（toggleBroadcast/broadcastState/broadcastEnabled/broadcastTargets/setBroadcastBarCollapsed/toggleBroadcastTarget/setBroadcastTargets/pruneBroadcastTargets/clearBroadcastState）+ stopSession delete 簇追加清理行；npx tsc --noEmit 通过；store 保持零 $effect 不变量。</done>
</task>

<task type="auto">
  <name>Task 2: 新增 BroadcastTargetSelector.svelte 受控组件 + 改造 EditPane 消费它（D-01, D-04, D-05 宿主契约）</name>
  <files>src/lib/components/BroadcastTargetSelector.svelte, src/lib/components/EditPane.svelte</files>
  <read_first>
    - src/lib/components/EditPane.svelte:1-14（imports：app store / SessionMinimap / SessionPreviewPopover 模式）
    - src/lib/components/EditPane.svelte:20-37（sessions $derived / selectedTabIds 本地 $state / prune $effect / toggle/selectAll/selectNone —— 这些全保留，仅 markup 替换）
    - src/lib/components/EditPane.svelte:135-182（DOM 结构：`.edit-pane` > `.editor-area` + `.session-panel`；141-168 是要抽出的列表+actions；170-176 是 EditPane 专属 .broadcast-btn 留下；179-181 hover popover 留下）
    - src/lib/components/EditPane.svelte:184-318（`<style>`：220-299 是随组件移走的 .session-list/.session-item/.session-item.selected/.session-meta/.session-type/.session-label/.select-actions/.link-btn/.empty-hint；201-218 .session-panel/.panel-header 与 301-318 .broadcast-btn 留 EditPane）
    - src/lib/components/SessionMinimap.svelte（被复用的缩略图组件，确认其 props 契约：接收 tabId）
    - .planning/phases/01-broadcast-ui-state/01-PATTERNS.md §Pattern Assignments §NEW BroadcastTargetSelector.svelte（受控 props 契约 + 逐字复制清单）
    - .planning/phases/01-broadcast-ui-state/01-UI-SPEC.md §Component Inventory §BroadcastTargetSelector（props 锁定）
  </read_first>
  <action>
    **步骤 A —— 新建 src/lib/components/BroadcastTargetSelector.svelte（per D-01）：**

    受控组件，自身零 `$state`/零 `$effect`。`<script lang="ts">` 内用 `$props()` 解构 5 个 prop（UI-SPEC 锁定契约）：
    - `sessions: SessionInfo[]`（宿主已过滤主标签，per D-05）
    - `selectedIds: Set<string>`（只读，组件不得原地改）
    - `onToggle: (tabId: string) => void`
    - `onSelectAll: () => void`
    - `onSelectNone: () => void`

    SessionInfo 类型从 `"../stores/app.svelte.ts"` import（沿 EditPane import 风格；若该类型未导出则用 `import("../stores/app.svelte.ts").SessionInfo` 内联类型）。import `SessionMinimap from "./SessionMinimap.svelte"`。import `t` from `"../i18n/index.svelte.ts"`（i18n key 在 Plan 01-02 加，本 task 先用 `t("ai.broadcast.empty")` 等占位 —— 注意：i18n key 尚不存在会导致运行时缺文案，但 t() 容错返回 key 本身；Plan 01-02 Task 2 会补齐 key，本 task 不应在此阻塞）。

    markup 逐字搬自 EditPane.svelte:141-168，替换：
    - `onclick={() => toggle(s.tabId)}` → `onclick={() => onToggle(s.tabId)}`
    - `onclick={selectAll}` / `onclick={selectNone}` → `onclick={onSelectAll}` / `onclick={onSelectNone}`
    - 字面量 `"No connected sessions"` → `{t("ai.broadcast.empty")}`；`All` → `{t("ai.broadcast.select_all")}`；`None` → `{t("ai.broadcast.select_none")}`
    - **移除** `onmouseenter`/`onmouseleave` hover 处理器（hover 预览是宿主专属，A1 —— ChatPanel 首版不带 hover；EditPane 保留自己的 hover 需在宿主侧包裹，见步骤 B）

    `<style>` 把 EditPane.svelte:220-299 的 `.session-list` / `.session-item` / `.session-item:hover` / `.session-item.selected`（accent halo，D-01 复用核心）/ `.session-meta` / `.session-type` / `.session-label` / `.select-actions` / `.link-btn` / `.link-btn:hover` / `.empty-hint` 全部搬入（Svelte scoped 样式随 DOM 走，Pitfall 4）。**逐字复制，不改 token**（`var(--accent)` / `color-mix(...)` 保持，per Pr3）。

    **步骤 B —— 改造 src/lib/components/EditPane.svelte（per D-01, D-04）：**

    1. import 增加 `import BroadcastTargetSelector from "./BroadcastTargetSelector.svelte";`（near EditPane.svelte:11-13）。
    2. **保留不动**：`selectedTabIds = $state<Set<string>>(new Set())`（:21，编辑器一次性发送语义，与 AI 广播状态不同寿命，**不进 ai store**）、`toggle`/`selectAll`/`selectNone`（:29-37）、prune `$effect`（:23-27，EditPane 本地 Set 独立 prune）、`broadcast()`（:49-54）、hover popover 相关 `hoveredTabId`/`hoverAnchor`/`onHover`/`clearHover`（:41-47, :179-181）。
    3. 把 `.session-panel` 内 141-168 的整块（空状态分支 + 终端列表 each 循环 + 全选/全不选 actions）替换为消费共享组件：
       `<BroadcastTargetSelector sessions={sessions} selectedIds={selectedTabIds} onToggle={toggle} onSelectAll={selectAll} onSelectNone={selectNone} />`
       <!-- planner-discipline-allow: class="session-list" -->
    4. **保留** `.panel-header`（:139，"Target Sessions" 标题）与 `.broadcast-btn`（:170-176，Broadcast(N) 立即发送按钮，per D-04 该按钮是 EditPane 专属不进共享组件）。
    5. EditPane 的 hover 预览：因共享组件不持 hover 状态，EditPane 若要保留 hover popover，需在 `.session-panel` 外层维持 `{#if hoveredTabId && hoverAnchor}<SessionPreviewPopover />{/if}`（:179-181 原样保留）。但 hover 触发（onmouseenter/onmouseleave）原本挂在 `.session-item` 上，现在 `.session-item` 移入共享组件 —— EditPane 的 hover 预览将暂时失效（onHover 无挂载点）。**这是可接受的回归**：UI-SPEC A1 将 hover 预览列为可选，ChatPanel 首版不带；EditPane 的 hover 预览失效不影响 Broadcast(N) 核心功能。若需保留，后续可在共享组件加可选 `onHover`/`onHoverLeave` prop（本阶段不做，YAGNI）。验收以 Broadcast(N) 按钮可点击且发送正常为准。
    6. EditPane `<style>` 移除已搬走的 class（.session-list/.session-item*/.session-meta/.session-type/.session-label/.select-actions/.link-btn/.empty-hint，原 :220-299），**保留** .session-panel/.panel-header/.broadcast-btn/.editor-area/.edit-pane 等宿主专属样式。

    禁止：用 `on:click`/`$:`/`export let`（R7）；把 selectedTabIds 搬进 ai store（它是 EditPane 一次性发送语义）；在共享组件内调 `ai.*` 或 `app.*`（受控组件零副作用）。
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/components/BroadcastTargetSelector.svelte` 成功（新组件文件存在）
    - `grep -c 'export default' src/lib/components/BroadcastTargetSelector.svelte` == 1（默认导出 Svelte 组件）
    - `grep -cE 'sessions|selectedIds|onToggle|onSelectAll|onSelectNone' src/lib/components/BroadcastTargetSelector.svelte` ≥ 5（5 个 prop 都出现）
    - `grep -c 'class:selected={selectedIds.has' src/lib/components/BroadcastTargetSelector.svelte` == 1（选中 halo 绑定保留）
    - `grep -c 'SessionMinimap' src/lib/components/BroadcastTargetSelector.svelte` ≥ 2（import + 使用）
    - `grep -c '\$state\|\$effect' src/lib/components/BroadcastTargetSelector.svelte` == 0（受控组件零内部状态/副作用）
    - `grep -c 'on:click\|export let\|^\s*\$:' src/lib/components/BroadcastTargetSelector.svelte` == 0（R7 合规）
    - `grep -c 'BroadcastTargetSelector' src/lib/components/EditPane.svelte` ≥ 2（import + 使用）
    - `grep -c 'class="session-list"' src/lib/components/EditPane.svelte` == 0（内联列表已移除，移入共享组件）
    - `grep -c 'broadcast-btn' src/lib/components/EditPane.svelte` ≥ 1（Broadcast(N) 按钮保留，per D-04）
    - `grep -c 'selectedTabIds = \$state' src/lib/components/EditPane.svelte` == 1（本地选中状态保留，未搬进 ai store）
    - `grep -c 'app.broadcastToSessions' src/lib/components/EditPane.svelte` == 1（broadcast() 立即发送逻辑保留）
    - `npm run build` 退出码 0（Svelte 编译通过，EditPane 改造无编译错误）
  </acceptance_criteria>
  <done>BroadcastTargetSelector.svelte 作为受控组件存在（5 props，零内部 $state/$effect，含 SessionMinimap + accent halo 样式）；EditPane.svelte 消费它且保留 Broadcast(N) 按钮 + 本地 selectedTabIds + broadcast() 立即发送逻辑（回归安全）；npm run build 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无外部信任边界 | 本计划是纯前端 in-memory 状态 + 组件抽取，零 invoke/零网络/零 IPC/零持久化。信任边界不适用。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `_broadcastByTab` 主标签关闭后 record 残留（内存泄漏 + 旧 tabId 被误引用） | low | mitigate | Task 1: stopSession delete 簇追加 `delete _broadcastByTab[tab_id];`（主标签关闭路径，D-11）。acceptance criterion 强制 grep 命中。 |
| T-01-02 | Tampering | mutator 对 Set 做 in-place .add()/.delete() 导致反应性静默失效（UI 不更新，用户以为未勾选） | low | mitigate | Task 1: 所有 targets mutator 强制"重建 Set + 整体替换 record + 重赋值 _broadcastByTab"三步；acceptance criterion grep 验证每 mutator 含 `new Set(` 与整体赋值。 |
| T-01-03 | Denial of Service | EditPane 改造后 Broadcast(N) 立即发送功能回归失效 | low | mitigate | Task 2: 保留 selectedTabIds 本地 $state + broadcast() + .broadcast-btn；acceptance criterion grep 验证 broadcastToSessions 调用保留；`npm run build` 保证编译通过。 |

本计划无 npm/pip/cargo 安装（零新依赖，RESEARCH §Package Legitimacy Audit N/A），故无 T-XX-SC 供应链威胁条目。
</threat_model>

<verification>
- `npx tsc --noEmit` 通过（store 类型正确）
- `npm run build` 通过（Svelte 编译通过，BroadcastTargetSelector + EditPane 改造无编译错误）
- store.svelte.ts 保持零 `$effect` 调用不变量
- EditPane 的 Broadcast(N) 按钮仍在（D-04），broadcast() 逻辑完整（回归安全）
</verification>

<success_criteria>
- ai/store.svelte.ts 存在 `_broadcastByTab` per-tab 广播状态 + 9 个 getter/mutator + stopSession teardown（D-10, D-11 主标签路径）
- BroadcastTargetSelector.svelte 作为受控组件存在，EditPane 消费它且 Broadcast(N) 行为不变（D-01, D-04）
- 所有 targets mutator 遵循 Set 重建 + 整体替换模式（Pitfall 1 规避）
- 本计划为 Plan 01-02（ChatPanel 广播开关 + 目标条）提供 state 层与组件层就绪条件
</success_criteria>

<output>
完成后创建 `.planning/phases/01-broadcast-ui-state/01-01-SUMMARY.md`
</output>

<artifacts_produced>
## Artifacts this phase produces（供 plan-review-convergence 漂移验证排除新符号）

**本计划（01-01）新增/修改的符号：**

state（src/lib/ai/store.svelte.ts）：
- `interface BroadcastState { enabled: boolean; barCollapsed: boolean; targets: Set<string>; }`
- `let _broadcastByTab: Record<string, BroadcastState>`（私有 $state）
- `const DEFAULT_BROADCAST: BroadcastState`
- getter: `broadcastState(tabId)`, `broadcastEnabled(tabId)`, `broadcastTargets(tabId)`
- mutator: `toggleBroadcast(tabId)`, `setBroadcastBarCollapsed(tabId, collapsed)`, `toggleBroadcastTarget(tabId, targetTabId)`, `setBroadcastTargets(tabId, ids)`, `pruneBroadcastTargets(tabId, activeTabIds)`, `clearBroadcastState(tabId)`
- teardown: `stopSession` 内 `delete _broadcastByTab[tab_id]`

组件（src/lib/components/BroadcastTargetSelector.svelte，新增）：
- default export BroadcastTargetSelector（受控 Svelte 5 组件）
- props: sessions / selectedIds / onToggle / onSelectAll / onSelectNone

EditPane（src/lib/components/EditPane.svelte，改造）：
- 移除内联 .session-list/.session-item/.select-actions markup 与对应 scoped 样式
- 新增 import + `<BroadcastTargetSelector>` 消费
- 保留：selectedTabIds / toggle / selectAll / selectNone / prune $effect / broadcast() / .broadcast-btn / .panel-header / hover popover

**Plan 01-02 将新增的符号（供收敛验证预登记）：**
- ChatPanel：`.broadcast-toggle` 按钮 + `.broadcast-bar` 容器 + `.bar-header`/`.bar-list` + prune `$effect` + 本地 `$derived`（bState/broadcastOn/selectedCount/sessions/totalCount）
- CSS：`.broadcast-toggle.on`（镜像 .danger-toggle.on 换 --accent）+ `.broadcast-toggle .badge`
- i18n key（en.ts + zh.ts）：`ai.toolbar.broadcast_enable` / `ai.toolbar.broadcast_on_tip` / `ai.toolbar.broadcast_aria` / `ai.broadcast.title` / `ai.broadcast.count` / `ai.broadcast.empty` / `ai.broadcast.select_all` / `ai.broadcast.select_none` / `ai.broadcast.collapse` / `ai.broadcast.expand`
</artifacts_produced>
